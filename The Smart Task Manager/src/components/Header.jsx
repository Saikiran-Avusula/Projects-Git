
import "../../src/App.css"; // Importing a CSS file for styling if needed

const Header = () => {
    return (
        <header className="header">
            <h1>Smart Task Manager</h1>
        </header>
    );
};

export default Header;
